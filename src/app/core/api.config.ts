/**
 * Base URL of the Raken backend — must include the global `/api` prefix.
 * NestJS serves all routes under `/api/*` (see `API_GLOBAL_PREFIX` in raken-backend).
 */
export const API_BASE_URL = 'https://raken-backend-nu.vercel.app/api';
